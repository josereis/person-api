# Dados de configuração do Banco de Dados
spring:
  datasource:
    url: jdbc:h2:file:/home/josereis/development/workspace-pessoal/db_h2_person
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
